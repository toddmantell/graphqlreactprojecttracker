import {h} from 'preact';
import styled from 'styled-components';

const MainDiv = styled.div`
  padding: 15px;
`;

const Layout = props => {
  return (
    <MainDiv>
      Application Root
    </MainDiv>
    );
}